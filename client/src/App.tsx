import React from "react";
import { ModeToggle } from "./components/mode-toggle";
const App = (): React.ReactNode => {
  return (
    <div>
      <ModeToggle />
    </div>
  );
};

export default App;
